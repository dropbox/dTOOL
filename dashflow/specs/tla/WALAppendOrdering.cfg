\* TLC Configuration for WALAppendOrdering
\* Part of TLA-005: Verify WAL append ordering

\* Model values for constants
CONSTANTS
    Writers = {w1, w2}
    MaxEvents = 3
    MaxEventId = 6

\* Initial state and next-state relation
INIT Init
NEXT Next

\* State constraint to bound state space
CONSTRAINT StateConstraint

\* Invariants to check
INVARIANTS
    TypeInvariant
    DurableIsPrefix
    UniqueEventIds
    UniqueDurableIds
    AppendOnlyDurable
    MutexExclusion
    SingleWriterOrder
    Safety

\* TLC options
\* Check for deadlock (disabled - terminal crash state is expected)
CHECK_DEADLOCK FALSE
