\* TLC Model Configuration for StateGraph
\* This file defines constants and checks for model checking

SPECIFICATION Spec

\* Model constants are defined in StateGraphMC.tla (instantiation of StateGraph.tla).

\* Check these invariants
INVARIANT TypeInvariant
INVARIANT RecursionLimitRespected
INVARIANT ValidCurrentNode
INVARIANT ValidExecutedNodes
INVARIANT Safety

\* Check these properties
PROPERTY EventuallyTerminates
PROPERTY NoLivelock

\* Termination states (status="completed"/"error_*") intentionally have no Next step.
CHECK_DEADLOCK FALSE
