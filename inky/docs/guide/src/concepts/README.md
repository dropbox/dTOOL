# Core Concepts

This section covers the basic building blocks of inky.

- Nodes define the UI tree.
- Layout uses flexbox rules to position nodes.
- Styles apply color, spacing, borders, and text attributes.
