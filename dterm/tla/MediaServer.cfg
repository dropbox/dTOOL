\* TLC Model Checking Configuration for MediaServer.tla

SPECIFICATION Spec

\* Use small constants for tractable model checking
CONSTANTS
    MaxTTSQueueDepth = 2
    MaxStreamDuration = 2
    MaxLatencyTicks = 1
    Clients = {"agent1"}
    AudioFormats = {"PCM"}
    NoClient = "NO_CLIENT"
    Texts = {"hi"}

\* Limit state space by constraining clock
CONSTRAINT Constraint

\* Invariants to check
INVARIANTS
    TypeInvariant
    SafetyInvariant
    SingleSTTSession
    TTSQueueBounded
    StreamClientsValid
    NoOrphanedProcessing
    IdleSTTNoClient
